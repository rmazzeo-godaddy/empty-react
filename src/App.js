import './App.css';
import 'h8k-components';

import React, { useState, useEffect } from 'react';

const title = "React App";

// 1) create an form for first/last/email
// 2) layout in column fashion with email taking 50% and the other fields at 25% each
// 3) add action button
// 4) add required validation for first last
// 5) add email validation
// 6) for all the below, try to create unit tests as you go
// 7) action button should only be clickable when valid
// 8) action button should see if a matching first/last exists in employees.js

const App = () => {
    return (
        <div className="App">
            <h1>page</h1>
        </div>
    );
}

export default App;
